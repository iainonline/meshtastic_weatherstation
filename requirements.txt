# Ultra-minimal dependencies for Pi Zero 2 W
# Uses Meshtastic CLI (installed via pip install meshtastic --no-deps)

# DHT22 Temperature/Humidity Sensor (optional)
adafruit-circuitpython-dht
adafruit-blinka
