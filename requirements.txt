# USB-only installation (no Bluetooth dependencies)
# Install meshtastic without deps to skip slow dbus-fast compilation
# Use: pip install --no-deps meshtastic==2.3.12
# Then: pip install -r requirements.txt

meshtastic==2.3.12
pyserial>=3.5
protobuf
pypubsub
dotmap
pexpect
tabulate
timeago

# DHT22 Temperature/Humidity Sensor
adafruit-circuitpython-dht
RPi.GPIO
